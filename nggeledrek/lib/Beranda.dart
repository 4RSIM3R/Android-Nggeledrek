import 'package:flutter/material.dart';

void main () => runApp(Beranda());

class Beranda extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Alhamdulillah Bisa'),
      ),
    );
  }

}